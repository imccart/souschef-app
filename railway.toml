[build]
builder = "nixpacks"
buildCommand = "pip install -e '.[web,kroger,postgres]'"

[deploy]
startCommand = "uvicorn souschef.web.app:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
